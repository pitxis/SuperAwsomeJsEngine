Drawable = function() {}
    
    this.canvasWidth = 0;
    this.canvasHeight = 0;
 
    this.init = function(x, y, w, h) {
        // Default variables
        this.x = x;
        this.y = y;
        this.width = w;
        this.height = h;
    }
 
    
    this.draw = function() {
    };

    this.update = function() {
    };
}